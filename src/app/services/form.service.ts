import { Injectable } from '@angular/core';
import { Observable } from 'rxjs';
import { Monster } from '../models'

@Injectable({
  providedIn: 'root'
})
export class FormService {

  constructor() { }

  submitMonster(): Observable<any> {
    return
  }
}
